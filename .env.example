# API base URL for frontend requests.
# Empty value means relative requests to the same origin (e.g. /api/*).
VITE_API_BASE_URL=

# Dev-only proxy target for Vite (`npm run dev`).
# Ignored in Docker/production build.
VITE_DEV_API_PROXY=http://127.0.0.1:8081
